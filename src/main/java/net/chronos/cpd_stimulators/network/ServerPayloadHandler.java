package net.chronos.cpd_stimulators.network;

import net.neoforged.neoforge.network.handling.IPayloadContext;

public class ServerPayloadHandler {

    public static void handleData(final AirdropPos data, final IPayloadContext context) {
//        // Do something with the data, on the network thread
//        CPDStimulators.LOGGER.info(data.name() + " SERVER");
//
//        // Do something with the data, on the main thread
//        context.enqueueWork(() -> {
//                    CPDStimulators.LOGGER.info(data.age() + " SERVER");
//                })
//                .exceptionally(e -> {
//                    // Handle exception
//                    context.disconnect(Component.translatable("my_mod.networking.failed", e.getMessage()));
//                    return null;
//                });
    }
}
